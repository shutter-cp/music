<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.looc.music.dao.LoginDao">
	<!-- user登陆 -->
	<select id="userLogin" parameterType="com.looc.music.bean.UserBean"  resultType="com.looc.music.bean.UserBean">
			SELECT
				userID,
				username,
				`password`,
				tel,
				email,
				lv,
				img,
				points
			FROM
				users
			WHERE
				username = #{username}
			AND
				password = #{password}
	
		</select>
		<!--  用户注册 -->
		<insert id="userAdd" parameterType="com.looc.music.bean.UserBean" flushCache="true">  
			INSERT INTO
				users(
					adm_userID,
					password,
					username,
					tel,
					email,
					lv,
					img,
					points
				)
			VALUES
				(
				#{adm_userID},
				#{password},
				#{username},
				#{tel},
				#{email},
				#{lv},
				#{img},
				#{points}
				)
	</insert>  
	
	<!-- admin登陆 -->
	<select id="adminLogin" parameterType="com.looc.music.bean.UserBean"  resultType="com.looc.music.bean.UserBean">
			SELECT
				userID,
				username,
				`password`,
				tel,
				email,
				lv,
				img,
				points
			FROM
				admin
			WHERE
				username = #{username}
			AND
				password = #{password}
		</select>
	
	<!-- authorLogin登陆 -->
	<select id="authorLogin" parameterType="com.looc.music.bean.UserBean"  resultType="com.looc.music.bean.UserBean">
			SELECT
				userID,
				username,
				`password`,
				tel,
				email,
				lv,
				img,
				points
			FROM
				author
			WHERE
				username = #{username}
			AND
				password = #{password}
		</select>
	
	
</mapper>  