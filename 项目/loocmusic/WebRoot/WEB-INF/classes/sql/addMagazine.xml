<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.looc.music.dao.AddMagazineDao">
		<!--  写期刊 -->
		<insert id="addMagazineDao" parameterType="com.looc.music.bean.AddMagazienBean" flushCache="true">  
			INSERT INTO
				magazine(
					musicID,
					userID,
					img,
					text,
					commentTime,
					approval,
					title
				)
			VALUES
				(
				#{musicID},
				#{userID},
				#{magazineImg},
				#{magazineText},
				NOW() ,
				'0',
				#{magazineTitle}
				)
	</insert>  
	
	<!-- 写入音乐 -->
	<insert id="addMusicDao" parameterType="com.looc.music.bean.AddMagazienBean" flushCache="true">  
			INSERT INTO
				music(
					musicID,
					styleID,
					img,
					weight,
					mpoints,
					src,
					`name`,
					musicText
				)
			VALUES
				(
				#{musicID},
				#{styleName},
				#{musicImg},
				'0',
				'0',
				#{musicSrc},
				#{name},
				#{musicText}
				)
	
	</insert>  
	
</mapper>  